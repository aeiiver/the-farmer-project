package root.controller;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.*;

class CompteCtrlTest {

  /**
   * Ce test doit vérifier si la modification des informations utilisateur
   * est effective dans la base de données.
   */
  @Test
  void updateUtilisateur() {
  }

  /**
   * Ce test doit vérifier que la modification des informations utilisateur
   * ne se fasse pas avec des valeurs illégales.
   *
   * <p>Un champ a une valeur illégale quand celle-ci ne respecte pas le format attendu par
   * le champ</p>
   *
   * <p>Il faudra vérifier que à la modification, on capture bien une exception.
   */
  @Test
  void valeursIllegales() {
    // assertThrows() things
  }

}
