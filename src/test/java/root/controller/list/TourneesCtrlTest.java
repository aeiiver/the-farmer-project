package root.controller.list;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.*;

class TourneesCtrlTest {

  /**
   * Ce test doit vérifier que l'ajout d'une tournée est effectif dans la liste des tournées.
   *
   * <p>Après ajout, on peut vérifier si dans la liste des tournées, on retrouve bien
   * la tournée ajoutée.</p>
   */
  @Test
  void ajouterTournee() {
    //TODO
  }

  /**
   * Ce test doit vérifier que la suppression d'une tournée est effective dans la liste des
   * tournées.
   *
   * <p>Après suppression, on peut vérifier si dans la liste des tournées, on ne retrouve pas
   * la tournée supprimée.</p>
   */
  @Test
  void supprimerTournee() {
    //TODO
  }

  /**
   * Ce test doit vérifier que la modification d'une tournée est effective dans la liste des
   * tournées.
   *
   * <p>Après modification, on peut vérifier si dans la liste des tournées, on retrouve bien
   * la tournée modifiée avec les modifications apportées.</p>
   */
  @Test
  void editerTournee() {
    //TODO
  }

}
