package root.controller;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.*;

class ProducteursCtrlTest {

  /**
   * Ce test doit vérifier que l'ajout d'un producteur est effectif dans la liste
   * des producteurs.
   *
   * <p>Après ajout, on peut vérifier si dans la liste des producteurs,
   * on retrouve bien le producteur ajouté.</p>
   */
  @Test
  void ajouterProducteur() {
  }

  /**
   * Ce test doit vérifier que la suppression d'un producteur est effective dans la liste
   * des producteurs.
   *
   * <p>Après suppression, on peut vérifier si dans la liste des producteurs,
   * on ne retrouve pas le producteur supprimé.</p>
   */
  @Test
  void supprimerProducteur() {
  }

  /**
   * Ce test doit vérifier que la modification d'un producteur est effective dans la liste
   * des producteurs.
   *
   * <p>Après modification, on peut vérifier si dans la liste des producteurs,
   * on retrouve le producteur avec les modifications apportées.</p>
   */
  @Test
  void editerProducteur() {
  }

}
