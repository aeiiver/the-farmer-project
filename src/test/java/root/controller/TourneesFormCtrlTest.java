package root.controller;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.*;

class TourneesFormCtrlTest {

  /**
   * Ce test doit vérifier que l'enregistrement est effectif dans la liste des tournées.
   */
  @Test
  void enregistrer() {
  }

  /**
   * Ce test doit vérifier que l'enregistrement n'est pas effectif dans la liste des tournées.
   */
  @Test
  void cancel() {
  }

  /**
   * Ce test doit vérifier que l'enregistrement d'une tournée ne se fasse
   * pas avec des valeurs de champs illégales.
   *
   * <p>Un champ a une valeur illégale quand celle-ci ne respecte pas le format attendu par
   * le champ</p>
   *
   * <p>Il faudra vérifier que à l'enregistrement, on capture bien une exception.
   */
  @Test
  void valeursIllegales() {
    // assertThrows() things
  }

}
